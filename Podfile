platform :ios, '10.0'

# enable to check bundling of resources
use_frameworks!

source 'https://github.com/CocoaPods/Specs.git'
target "SCIBarcodeScannerExample" do
    pod 'SCIBarcodeScanner', :path => '.'
end
